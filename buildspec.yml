version: 0.2

phases:
  install:
    commands:
      - npm i npm@latest -g
      - pip install --upgrade pip
      - pip install --upgrade awscli
  pre_build:
    commands:
      - echo Pre_build Phase
      - npm install
  build:
    commands:
      - echo Build Phase
      - npm run build:static
  postt_build:
    commands:
      - echo PostBuild Phase
      - aws s3 sync ../public/static $S3_BUCKET
